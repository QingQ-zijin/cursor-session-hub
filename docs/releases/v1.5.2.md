v1.5.2：精简界面，专注会话阅读与整理。

- 移除 New Chat、Search、Customize 等冗余导航和 API 聊天；同步集中到顶部一个入口，任务与导出下载使用相邻的下载图标。
- 支持 Ctrl/Cmd + 加减号调整界面缩放，Ctrl/Cmd + 0 恢复 100%，自动记住比例。View 菜单保留主题和缩放设置。
- 「更早的会话」移动到右侧目录，每页最多 100 轮、最多同时展开三轮；窄屏使用可收起的右侧抽屉。
- 轮次支持移到回收站、添加删除说明、修改备注和恢复。团队会话仅所有者及管理员可修改，原始 Cursor 文件保持只读。
- 工具与执行过程默认合并收起；有原始时间时显示 Worked for 时间跨度，否则显示记录数量。展开后才读取工具详情，问答文字仍完整展示。
- 新导出与新同步排除已删除轮次，保留 Markdown、HTML、PDF，以及已有账号、评论、收藏和静态更新检测。

已有安装直接升级，网页端刷新使用已部署版本。轮次删除按版本管理，不清除已有下载文件或历史同步版本。详见仓库 docs/ROUND_MANAGEMENT.md。
