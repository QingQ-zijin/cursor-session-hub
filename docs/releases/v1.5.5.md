v1.5.5：公式兼容与导入界面精简。

- 阅读器支持 `\(...\)` 行内公式和 `\[...\]` 块公式，保留原有 `$...$`、`$$...$$`；兼容带明确 LaTeX 命令的独立方括号公式行。
- 公式转换只用于显示，不改写原始会话。代码块、行内代码、链接及转义字面量保持原样。HTML 导出使用相同处理，公式资源继续离线打包。
- 导入窗口改为紧凑布局：蓝色区分工作区、操作与选中项，绿色标识已入库，琥珀色标识更新，异常用红色标识；同步全部和导入所选集中在底部。
- 移除重复刷新入口、导入说明段落、阅读区脚注和冗长提示，保留操作标签、进度和必要错误状态。适配深浅色与窄屏。
- 保留工作区批次同步、最终回答聚焦、缩放、回收站和三种导出格式。

现有记录无需重新导入。旧的已生成 HTML 文件需重新导出才能应用新版公式处理。PDF 继续保留公式源码，本版不新增 PDF 的 LaTeX 图形排版。
