修复 Markdown 下载扩展名，并新增 PDF 导出。

- 选择“导出 Markdown”后，默认保存为 `.md`，不再错误显示为 `.html`。任务列表、保存窗口文件类型和下载内容保持一致。
- 下载文件使用会话标题命名；已有导出任务也能根据原始格式返回正确扩展名。
- 会话操作菜单新增“导出 PDF”，完成后在“同步任务”中点击“下载 PDF”。支持内置中文字体、问答、标题、表格、代码、已保存图片和自动分页。
- PDF 在后台任务中按索引逐批生成，保留原有单任务和内存限制。数学公式保留原始 LaTeX 文本；需要公式排版时可使用 HTML 导出。
- 安装包内置 PDF 所需字体和组件，不需要安装办公软件或联网下载字体。保留原有记录、账号和同步配置。

升级到 1.1.1 后生效；服务器网页端刷新即可使用部署后的版本。Windows、Mac Apple Silicon、Mac Intel 分别使用对应安装包。
