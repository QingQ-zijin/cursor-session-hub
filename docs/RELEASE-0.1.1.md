# 0.1.1 内测更新

客户端可连接管理员明确启用的公网 HTTP 内测地址。默认仍要求 HTTPS；输入 HTTP 地址后，用户需勾选“允许 HTTP 内测连接”。同意只针对当前完整服务器地址，切换地址后需要重新选择，保存配置中不含密码或同步凭证。

修复 HTTP 网页上复制邀请链接、会话链接不可用的问题。安全上下文优先使用 Clipboard API，其余情况使用点击触发的复制方式。

本地回归：64项后端测试及4项前端测试通过。部署端口和安装包启动检查结果随最终交付记录提供。内测使用 HTTP 的决定不代表连接已经加密。

最终三平台安装包来自提交 `bde53fe11d9c1bb190525e27268e2b9679f61698`，Windows x64、Mac Apple Silicon、Mac Intel均通过当前0.1.1版本的实际安装或挂载启动检查：[CI运行34246661041](https://github.com/QingQ-zijin/cursor-session-hub/actions/runs/34246661041)。测试不会再从构建缓存误选旧版安装包。

实际部署验收：公网API9项通过；Chrome与Edge各7项通过，包含登录、成员管理、HTTP复制邀请与退出。Windows实际安装后9项通过，覆盖中文路径导入、冻结解析器、最多3轮阅读、HTTP同意设置、系统凭据存储、原生手动同步、网页另一个账号读取/评论及客户端读回评论，且正常退出。原生OS文件选择操作未自动化，测试经受控原生接口提供路径；Mac的本次验收范围是构建、冻结解析器及安装后启动。
