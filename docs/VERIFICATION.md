# 0.1 内测验收记录

以下均为实际运行结果，不代表生产环境SLA。未租云服务器时，在本机Docker中用合成数据模拟协作；真实长JSONL只用于本地文字校验，不上传GitHub。

| 检查 | 结果 |
|---|---|
| 原始长JSONL → 本地索引 → 同步包 → 云端索引 | 949段AI文字及顺序逐段一致 |
| 100,000条／122.63MiB源文件流式索引 | 209.74秒；工作进程树峰值55.92MiB，10%进度55.70MiB、结束55.92MiB |
| 5个并发索引读取者 | 500次查询，P95 16.01毫秒 |
| 实际Docker HTTP并发 | 5成员分别阅读1000事件/500轮，2个上传并行；913请求，P95 27.27毫秒 |
| 实际Docker资源边界 | 95次采样中运行解析任务/进程均最多1个；最大响应29,331字节 |
| 完整双账号协作 | 13项通过：同步、作者筛选、文字、图片、评论、私有收藏、权限、停用、导出、撤销与重新共享 |
| 备份恢复 | 8项通过；9张表及1046内容文件校验一致；非空目标恢复被拒绝 |
| 网页阅读 | 真实204轮记录初始3轮/20事件/701DOM节点；工具正文未提前生成 |
| 网页协作 | 管理员/成员登录、成员筛选、评论、收藏定位及认证图片通过 |
| 手机窄屏 | 390px无横向溢出；深色模式与最多3轮限制正常 |
| Windows系统凭证存储 | 实际写入、读取、删除均通过；无明文配置回退 |
| 产品后端回归 | 54项通过；前端4项测试及TypeScript生产构建通过 |
| Windows x64安装包 | Windows CI实际安装NSIS包，冻结解析器与桌面启动检查通过 |
| Mac Apple Silicon安装包 | macOS 14 CI实际挂载DMG，冻结解析器与桌面启动检查通过 |
| Mac Intel安装包 | macOS 15 Intel CI实际挂载DMG，冻结解析器与桌面启动检查通过 |
| 生产部署配置 | 实际Caddy容器配置校验通过；Compose展开验证HTTPS来源、Secure Cookie、端口隔离及CPU/内存限制 |

新增产品测试覆盖权限、邀请、会话版本、上传校验和恢复、解析异常、超大记录、进程退出与资源上限、迁移幂等，以及无Content-Length请求的真实流式大小限制。

完整旧版unittest共194项：189通过、1项可选浏览器检查跳过、4项Windows路径相关失败。已在未改动的原查看器复现同4项失败；产品自己的路径授权和分页测试另外通过。

三个安装包来自提交 `4070b25b04037cf58555ce1d084c1c3ce1d1114f`，最终[CI运行34225940723](https://github.com/QingQ-zijin/cursor-session-hub/actions/runs/34225940723)的测试及三个打包任务全部成功。安装包的SHA256校验值随本地交付文件提供；后续交付文档和CI诊断文件收集改动不改变这三个安装包的应用代码。

边界：内测包未进行商业签名或Apple公证；已开始的下载无法收回已发送字节，但撤销共享后新请求会被拒绝。安装包在CI对应系统进行启动测试，团队实际设备差异仍应在内测中验证。

## 首个公开 Release 0.1.3

2026-09-08，提交 `15bc0cf7a6b2c6f9e550d5b8866af863d2d63e76` 的 [CI 34260091121](https://github.com/QingQ-zijin/cursor-session-hub/actions/runs/34260091121) 全部成功。已公开发布 [v0.1.3](https://github.com/QingQ-zijin/cursor-session-hub/releases/tag/v0.1.3)，三个安装包和许可证均按发布的 SHA256SUMS.txt 在本地下载后核验一致。

- 88项后端测试、4项前端单元测试、TypeScript生产构建通过。
- 6项真实浏览器回归通过：本地切云端加载状态、快速切回后忽略旧响应、加载失败重试、带空间标记的链接及过期响应、自动更新提示、离线检查恢复。
- 三个平台各自验证冻结解析器及安装/挂载后启动，应用报告版本均为0.1.3。
- 现有云端完成数据库备份与升级，4项浏览器检查通过：登录/索引、管理员删除入口、返回团队列表、注销。没有新建测试成员或上传实际会话。
- 匿名访问已发布GitHub Release，验证当前0.1.3不误报更新、旧版本可检测更新，Windows x64、Mac ARM、Mac Intel下载链接均匹配。

上述为本次新增检查。完整长会话及负载数据见前面的历史验收记录，本次未重复运行未受改动影响的压力测试。Mac验证限于CI构建、冻结解析与启动，不等同于三台实际用户电脑的完整交互验收。

## v1.1（1.1.0）

应用提交 `a4470c39ec59a82e85a244ae0b33ee544523341b`，对应 [CI 34307136752](https://github.com/QingQ-zijin/cursor-session-hub/actions/runs/34307136752)。[v1.1.0 Release](https://github.com/QingQ-zijin/cursor-session-hub/releases/tag/v1.1.0) 已发布。

- 100项后端、4项前端单元测试、8项真实浏览器回归全部通过，TypeScript生产构建通过。
- Windows x64、Mac Apple Silicon、Mac Intel 均通过目标系统构建、实际安装/挂载后启动；每个平台的冻结Python程序还实际完成Markdown/HTML导出再导入、PDF诊断页导入及离线公式资源检查。
- 本机只读扫描423个来源、12个工作区，主标题中的UUID数量为0。默认隐藏与IDE同ID的JSONL副本；测试覆盖工作区分页、筛选、备用来源与自定义标题保护。
- 原始长JSONL的949段AI文字在HTML导出再导入后逐段一致，Markdown导出中内容与顺序一致。真实数据仅在本地验证，未进入Git或Release附件。
- 合成HTML导出在离线Chrome中显示2个公式、1张表格、1段代码，无外部网络请求。
- 浏览器回归验证展开轮次超过40条消息时自动读取后续页、超过20个内容块和24000字符仍完整展示、正文分块自动合并、大消息按结构展示而非JSON文本；未展开的旧轮次不请求正文。
- 现有服务器在数据库备份后升级为1.1.0，5项浏览器验收通过，包括实际长会话的最近3轮自动完整读取。没有新增测试账号。
- 公开GitHub更新接口已验证：0.1.3识别1.1.0为新版，三个系统对应安装包链接正确，当前1.1.0不误报更新。

PDF支持文字提取，按页建立阅读目录；无文字层的扫描页保留诊断，需外部OCR。PDF导入64MiB上限、单文本块8MiB上限；异常不标记为完整解析。原有512MiB解析进程限制及分页接口响应上限保留。Mac检查范围仍为CI中的冻结解析、导出及启动，不等同于所有实际Mac设备的完整人工验收。

## 1.1.1：下载格式与 PDF 导出

发布 [v1.1.1](https://github.com/QingQ-zijin/cursor-session-hub/releases/tag/v1.1.1)，应用提交 `d8f18c660e815f06ff59d2361689e8c67771dff5`。[CI 34316491034](https://github.com/QingQ-zijin/cursor-session-hub/actions/runs/34316491034) 的107项后端、4项前端单元、11项浏览器测试及三平台打包全部通过。

- 验证 Markdown、HTML、PDF 的任务格式、下载扩展名、MIME和保存类型一致；已有Markdown任务的扩展名正确，不公开服务器文件路径。
- 三个平台的冻结程序都实际生成PDF并通过中文与代码文字提取检查，安装/挂载后启动通过。
- PDF样本的3页均经Poppler渲染检查，包括中文、加粗、表格、图片、跨页长代码和页码。修正了初版代码行背景遮挡后重新渲染核对。
- 真实2727条记录导出1465页PDF，文件约4.3MB；独立进程树峰值132.28MiB，耗时36.14秒，低于512MiB边界。数据未进入Git或Release附件。
- 云端备份数据库后升级1.1.1；通过实际Chrome菜单导出和下载三种格式，检查默认扩展名及PDF文件签名；PDF可提取中文原文。没有创建测试账号。
- PDF内嵌Noto Sans SC字体子集，字体以SIL OFL 1.1分发。PDF数学公式保留LaTeX原文，不宣称与HTML公式排版相同。
- 公开更新接口确认旧版可检测1.1.1，Windows和两种Mac下载地址匹配。
