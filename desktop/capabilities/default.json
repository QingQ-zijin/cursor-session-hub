{
  "identifier": "main-window",
  "description": "Only the bundled application may invoke narrowly scoped native operations.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
